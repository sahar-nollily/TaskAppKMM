CREATE TABLE  task_Entity(
    id INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    title TEXT NOT NULL
);

selectAll:
SELECT *
FROM task_Entity;

insertTask:
INSERT OR REPLACE
INTO task_Entity (
    id,
    title
) VALUES (?, ?);
