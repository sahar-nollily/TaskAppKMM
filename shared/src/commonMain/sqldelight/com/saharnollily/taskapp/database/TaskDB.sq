CREATE TABLE  task_Entity(
    id INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    title TEXT NOT NULL UNIQUE
);

selectAll:
SELECT *
FROM task_Entity;

insertTask:
INSERT
INTO task_Entity (
    id,
    title
) VALUES (?, ?);
